        <model name="{{model_name}}">
            <pose> {{x}} {{y}} {{z}} {{roll}} {{pitch}} {{yaw}} </pose>
            <link name="link">
                <visual name="visual">
                    <geometry>
                        <{{shape_type}}>
                            {{size}}
                        </{{shape_type}}>
                    </geometry>
                    <material>
                        <ambient> {{color}} 1 </ambient>
                        <diffuse> {{color}} 1 </diffuse>
                        <specular> {{color}} 1 </specular>
                        <emissive> {{color}} 1 </emissive>
                    </material>
                </visual>
            </link>
        </model>